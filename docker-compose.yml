version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: fi-mcp-dev
    container_name: fi-mcp-dev
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./test_data_dir:/app/test_data_dir
    restart: unless-stopped